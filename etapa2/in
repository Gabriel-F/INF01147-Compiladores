
int a[1^10^2];
int foo(){
	int a;
	a = 1 + 2 + a / b + !a;
	a = (1 + 2) + !(false && true) + b;
	b = a[(1+2)^foo(var,b,1+2,false,(1+2)/2)];
	bool boolean <= false;
	
	/*
		ERROS:
		int a <= 2 +;
		int b <= 2 + 3;
		erro = c++;
	*/

	int a;
	
	if (6 || false) then {
		int a <= 2;	
	};

	if (1 <= 2) then {
		int t <= 1;
		t = 1 + t;
	}else{
		int t <= 2;
		t = 2 + t;
	};

	int contador <= 0;
	while(!(loop || false)){
		contador = contador + 1;	
	};
	
	return 0;
		
}
